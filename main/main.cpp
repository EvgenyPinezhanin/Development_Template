#include<iostream>
#include<Menu.h>

using namespace std;

int main() {
	menuText menu;
	menu.display();
	//Stack<Node*> *stack = new Stack<Node*>;
	//Node * nd = new Node;
	//stack->push(nd);
	return 0;
}
